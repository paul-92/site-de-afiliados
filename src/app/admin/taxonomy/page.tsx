import { createCategoryAction, createTagAction } from "@/admin/actions";
import { Notice } from "@/admin/components";
import { withAdminRepository } from "@/admin/repository";
export default async function TaxonomyPage({ searchParams }: { searchParams: Promise<{ notice?: string }> }) { const refs = await withAdminRepository((repo) => repo.references()); const query = await searchParams; return <section><h2>Categorias e tags</h2><Notice text={query.notice}/><div className="two-col"><article className="card"><h3>Categorias</h3><ul className="clean-list">{refs.categories.map((item) => <li key={item.id}><span>{item.name}<small>{item.slug}</small></span><span>{item.active ? "Ativa" : "Inativa"}</span></li>)}</ul><form action={createCategoryAction} className="stack-form"><label>Nome<input name="name" required/></label><label>Slug<input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*"/></label><label>Descrição<textarea name="description"/></label><label className="check"><input type="checkbox" name="active" defaultChecked/>Ativa</label><button>Criar categoria</button></form></article><article className="card"><h3>Tags</h3><ul className="clean-list">{refs.tags.map((item) => <li key={item.id}><span>{item.name}<small>{item.slug}</small></span></li>)}</ul><form action={createTagAction} className="stack-form"><label>Nome<input name="name" required/></label><label>Slug<input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*"/></label><button>Criar tag</button></form></article></div></section>; }
