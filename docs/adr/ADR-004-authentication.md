# ADR-004 — Authentication

Status: FROZEN. Use Supabase Auth for administrators only; public visitors remain anonymous. Every administrator authorization decision is enforced server-side, behind a replaceable boundary.
