# PLAN-007 Evidence — 2026-09-13

## Gate

**SPEC-007:** IMPLEMENTED · AWAITING HUMAN G3/G4  
**STOP CONDITION:** none encountered  
**Branch:** `feature/spec-007-public-frontend`  
**Accepted baseline:** `724cb845d7e7c24903428a21b5a7739653beffe9`

Baseline audit confirmed the official origin, exact clean SPEC-006 commit, and local-only `feature/spec-006-admin-cockpit`. A read-only remote-head check returned neither SPEC-006 nor SPEC-007. No push, PR, merge, or deploy occurred.

## Delivered routes

- `/` — hero, featured, dynamic categories, Até R$30, Até R$50, newest, editorial positioning.
- `/categoria/[slug]` — validated active category, public products, bounded pagination, empty/not-found states.
- `/produto/[slug]` — editorial detail, latest reference price, verification date, tags, affiliate and price disclosures.
- `/buscar` — GET search, trimmed and limited to 120 characters, over title, description, category, and tags.
- `/achados`, `/ate-30`, `/ate-50`, `/novidades` — server-rendered dynamic collections.
- `/go/[productSlug]` — existing SPEC-005 server-side tracking/redirect, now receiving allowlisted internal contexts.

Global `loading.tsx`, `error.tsx`, and `not-found.tsx` cover loading, query failure, and unavailable resources without technical leakage.

## Components and boundaries

`ProductCard`, `ProductGrid`, `PriceDisclosure`, `CatalogUnavailable`, `PageIntro`, and `CollectionPage` are reusable. Pages are Server Components; only the mandatory error retry boundary is client-side. No new dependency was added.

Publication eligibility remains centralized in the existing repository boundary: ACTIVE product, active category, active marketplace, and at least one active AffiliateLink. Latest price is selected by descending `observedAt`. Price buckets use that latest observation. Novidades uses deterministic descending product `createdAt`, then title.

Commercial HTML contains only same-site `/go/{slug}?source={allowlisted}` routes. Destination URLs are still selected and validated server-side by SPEC-005. Source contexts added: `FEATURED`, `PRICE_BUCKET`, and `NEWEST`; arbitrary values fall back to referrer-derived classification.

## Accessibility and responsive evidence

- Semantic header, navigation landmark with accessible name, main target, sections/articles, and footer.
- Skip link, visible `:focus-visible` treatment, labeled search, meaningful image alt text, named product links, announced empty/loading/error states, and reduced-motion support.
- CSS breakpoints cover mobile (single-column), tablet (two-column), and desktop (four-column); controls reflow without requiring pointer-only interaction.
- Automated storefront tests check landmarks, navigation label, skip link, focus rules, image alternative, CTA name/destination, and announced empty state.

## Verification

| Gate | Result |
|---|---|
| Lint | PASS — zero warnings |
| Type-check | PASS |
| Full regression | PASS — 37/37 tests, 10/10 files |
| Production build | PASS — all public/admin/tracking routes compiled |
| Migration validation | PASS — `drizzle-kit check` |
| Schema/migration diff | PASS — none |
| Secret scan | PASS — no credential, token, private key, or JWT value found; scanner hits were variable/test/documentation terms only |
| Runtime dependency audit | PASS — 0 vulnerabilities |
| Full dependency audit | 4 moderate advisories in legacy `drizzle-kit` development chain; unchanged from SPEC-006, absent from runtime; suggested fix is a breaking downgrade |
| Diff whitespace check | PASS |

The Vitest run emits a forward-looking Vite config-loader warning; it does not fail tests and is unrelated to runtime behavior.

## Scope and data

No schema, migration, seed, ADR, secret, analytics, advanced SEO, CMS/blog, login, favorites, review, scraping, campaign, marketing-agent, or automated research change. AffiliateLink is not exposed as a public destination.

## Local commits

- `45daf90` — `docs(spec-007): authorize public frontend`
- `0a37365` — `feat(spec-007): implement public storefront`
- final evidence commit follows this record
