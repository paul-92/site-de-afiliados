# SPEC-003 — Architecture

Status: ACCEPTED/DONE.

The foundation must support a fast, indexable public site, protected administrator boundary, relational catalog, affiliate redirect tracking, and basic analytics while remaining a low-cost modular monolith. The frozen stack and decisions are recorded in ADR-001–007. This document records the approved implementation context; it does not expand the contract into catalog or tracking behavior.
