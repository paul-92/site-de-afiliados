import{expect,test}from"@playwright/test";test("mobile storefront remains navigable",async({page})=>{await page.goto("/");await expect(page.getByRole("navigation",{name:"Navegação principal"})).toBeVisible();for(const path of ["/categoria/organizacao","/produto/organizador-giratorio","/buscar","/privacidade","/termos","/afiliados"]){await page.goto(path);await expect(page.getByRole("heading",{level:1})).toBeVisible()}await page.screenshot({path:"docs/evidence/SPEC-011/home-mobile.png",fullPage:true})});
