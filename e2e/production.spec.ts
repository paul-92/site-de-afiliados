import{expect,test}from"@playwright/test";test("production-like mode fails closed with DEMO requested and no database",async({page,request})=>{const errors:string[]=[];page.on("pageerror",e=>errors.push(e.message));await page.goto("/");await expect(page.getByText("Catálogo temporariamente indisponível")).toBeVisible();await expect(page.getByText("Organizador giratório compacto")).toHaveCount(0);await page.goto("/privacidade");await expect(page.getByRole("heading",{name:"Privacidade"})).toBeVisible();const sitemap=await(await request.get("/sitemap.xml")).text();expect(sitemap).not.toContain("demo-");expect(sitemap).not.toContain("organizador-giratorio");const headers=(await request.get("/termos")).headers();expect(headers["content-security-policy"]).toContain("frame-ancestors 'none'");expect(errors).toEqual([])});
